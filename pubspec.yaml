name: bioresonanz_app
description: Frequenzgenerator App
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.5.1
  flutter_lints: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
